import {
  Modules
} from '../../common.ts'

export interface Config {
  title?: string
  subtitle?: string
  after?: string
  icon?: string
  iconColor?: string
  iconUseState?: boolean
  item?: string
  modules?: Modules
}

export {
  Modules
}
